#include <stdio.h>

size_t strlen(int x);

int main(void)
{
    int x= 0;

    strlen(x);
    foor(x);

    return (0);
}

size_t strlen(int x)
{
    printf("strlen %d\n", x);

    return(0);
}


size_t foor(int x)
{
    printf("foor %d\n", x);

    return(0);
}